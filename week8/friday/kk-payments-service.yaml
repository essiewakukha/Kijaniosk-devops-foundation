apiVersion: v1
kind: Service
metadata:
  name: kk-payments-service
  namespace: default
  labels:
    app: kk-payments
spec:
  type: NodePort
  selector:
    app: kk-payments
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30001
