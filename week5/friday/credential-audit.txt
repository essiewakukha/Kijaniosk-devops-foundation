# CREDENTIAL AUDIT REPORT

Date: 2026-04-19
Pipeline: kijanikiosk-payments CI Pipeline
Branch: feature/week5-ci-pipeline

---

 CHECK 1: Credentials in Jenkinsfile

Command:
grep -iE "password|secret|token|key|auth" week5/friday/Jenkinsfile

Result: PASS

Finding:
Only variable references detected (withCredentials, NEXUS_PASS).
No hardcoded credentials present in the Jenkinsfile.
All sensitive values are retrieved securely from Jenkins Credentials store
using ID: nexus-credentials.

---

CHECK 2: Credentials in Git History

Command:
git log -p --all | grep -iE "password|secret|token|NEXUS_PASS|admin"

Result: WARNING

Finding:
Credentials were identified in historical commits (Week 3 exercise only):

* DB_PASSWORD=s3cr3t-pr0d-p@ssword
* PAYMENTS_WEBHOOK_SECRET=whsec_XyZaBcDeFgHiJkLmNoPqRsTuV

Impact:
These credentials are unrelated to the Week 5 CI pipeline and were not
introduced in the current working branch (feature/week5-ci-pipeline).

Verification:
No credentials were added, modified, or exposed in any Week 5 commits.

Remediation Recommendation:
Remove historical secrets using:

* git filter-repo OR
* git filter-branch

---

 CHECK 3: Credentials in package.json

Command:
grep -iE "password|secret|token" week5/friday/kijanikiosk/package.json

Result: PASS

Finding:
No credentials found in package.json.
File contains only application metadata and scripts.

---

CHECK 4: .npmrc Exposure in Repository

Command:
git ls-files | grep .npmrc

Result: PASS

Finding:
.npmrc is NOT tracked in version control.
It is dynamically created during the Publish stage and removed immediately after use:

rm -f .npmrc

This prevents credential persistence.

---

CHECK 5: Environment Files (.env)

Command:
git ls-files | grep ".env"

Result: PASS

Finding:
No .env files are committed to the repository.
No environment-based credentials are exposed.

---

## SUMMARY

Checks Passed   : 4
Warnings        : 1 (historical credentials – non-impacting to current pipeline)
Critical Issues : 0

---

 FINAL VERDICT

The Week 5 CI pipeline is SECURE with respect to credential management.

✔ No hardcoded credentials in Jenkinsfile
✔ No credentials exposed in repository files
✔ No credentials persisted in build artifacts
✔ Sensitive data handled exclusively via Jenkins Credentials store
✔ Credentials masked during pipeline execution

The only identified issue relates to legacy credentials from a previous
exercise (Week 3), which do not affect the current pipeline but should
be removed from git history as a best practice.

Overall, the pipeline adheres to secure DevOps credential management principles.
