apiVersion: v1
kind: Service
metadata:
  name: kk-payments
  namespace: kijani-project
  labels:
    app: kk-payments
    project: kijanikiosk
spec:
  type: ClusterIP
  selector:
    app: kk-payments
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000

